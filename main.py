def hello():
	print('Hello world!')

if __name__ == '__main__':
	hello()

# git process
# > git clone [url]
# > git add [file]
# > git commit -m "[name]"
# > git push -u origin main
# > git push (same branch, subsequent push)